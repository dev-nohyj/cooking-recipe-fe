import Main from '@/components/profile/Main';

interface Props {}

const ProfilePage = ({}: Props) => {
    return (
        <main>
            <Main />
        </main>
    );
};

export default ProfilePage;
